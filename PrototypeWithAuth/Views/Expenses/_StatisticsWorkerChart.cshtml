@model StatisticsWorkerViewModel
@using PrototypeWithAuth.ViewModels

@for (int c = 0; c < Model.CategoryTypesSelected.Count; c++)
{
    <input type="hidden" asp-for="CategoryTypesSelected[c]" class="category-type-selected" />
}

<table class="table table-headerspaced table-noheaderlines table-hover ">
    <thead>
        <tr class="text-center">
            <th></th>
            <th>Name</th>
            <th>Number of Orders</th>
            <th>Number of Items</th>
            <th>Total</th>
            <th>Details</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var employee in Model.Employees)
        {
            <tr class="text-center">
                <th>
                    @{
                        var src = "/UserImages/profile_circle_big.png";
                        if (employee.Key.UserImage != null)
                        {
                            src = employee.Key.UserImage;
                        }
                    }
                    <div class="btn btn-link p-0 circular-profile-img circular-profile-img-65"><img id="user-image" src="@src" /></div>
                </th>
                <th>
                    @employee.Key.FirstName
                    @employee.Key.LastName
                </th>
                <th>
                    @employee.Value.Count()
                </th>
                <th>
                    @employee.Value.Select(r => r.Product).Distinct().Count()
                </th>
                <th>
                    @employee.Value.Sum(r => r.Cost)
                </th>
                <th>

                </th>
            </tr>
        }
    </tbody>
</table>