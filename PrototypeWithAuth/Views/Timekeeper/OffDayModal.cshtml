@model PrototypeWithAuth.ViewModels.OffDayViewModel
@using PrototypeWithAuth.AppData

<link href="~/css/datepicker.min.css" rel="stylesheet" type="text/css">
<script src="~/js/site.js"></script>
<div class="modal modal-document fader on-form-modal timekeeper-form " id="" @*role="dialog" aria-labelledby="Request"*@ data-backdrop="true">

    <div class="modal-dialog-centered mx-auto modal-sm  work-from-home-modal">
        <div class="modal-content d-inline-block modal-border-radius modal-box-shadow">
            <button type="button" class="close modal-close-padding modal-close-style" data-dismiss="modal" aria-label="Close" onclick="$(this).closest('.modal').modal('hide');">
                <span aria-hidden="true">&times;</span>
            </button>
            <form action="" id="myForm" method="post" enctype="multipart/form-data" style="height: 100%; overflow: auto;" class="modal-padding sickModal">
                <input type="hidden" asp-for="@Model.OffDayType" />
                <input type="hidden" asp-for="@Model.PageType" />
                <input type="hidden" asp-for="@Model.ToDate" />
                <input type="hidden" asp-for="@Model.FromDate" />
                <input type="hidden" asp-for="@Model.Month" />
                @{ var heading = AppUtility.GetDisplayNameOfEnumValue(Model.OffDayType.ToString());}
                <div class="modal-header border-0 border-bottom modal-header-padding">
                    <div class="container">
                        <div class="">
                            <div class="heading-1">Report @heading</div>
                        </div>
                        <div class="mt-5 mb-3">
                            <hr />
                        </div>
                    </div>
                </div>
                <div class="datepicker-here" data-range="true" id="vacation-dates"
                     data-multiple-dates-separator=" - " data-language='en'></div>
                <div class="modal-footer mt-4 border-0 ">
                    <div class="text-center m-auto">
                        <input type="submit" class="custom-button custom-button-font timekeeper-color between-button-margin " id="saveOffDay" value="Save" />
                        <button type="button" class="custom-cancel custom-button cancel-email" data-dismiss="modal" onclick="$(this).closest('.modal').modal('hide');">Cancel</button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>
@*<script>
        $('#vacation-dates').datepicker().data('datepicker').selectDate(new Date("@Model.SelectedDate.Year-@Model.SelectedDate.ToString("MM")-@Model.SelectedDate.ToString("dd")"))
    </script>*@
<script src="~/js/datepicker.min.js"></script>
<!-- Include English language -->
<script src="~/js/datepicker.en.js"></script>

